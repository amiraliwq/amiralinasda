"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {createClient} from "../../lib/supabase/client"; import {ShoppingBag} from "lucide-react";
export default function ProductsPage(){const [products,setProducts]=useState<any[]>([]);useEffect(()=>{createClient().from("products").select("id,title,slug,price,image_url,category,featured").eq("active",true).order("featured",{ascending:false}).order("created_at",{ascending:false}).then(({data})=>setProducts(data||[]))},[]);return <main dir="rtl" className="academy-shell"><header className="academy-header"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5"><Link href="/" className="font-black">فروشگاه و آکادمی امیرعلی</Link><div className="flex gap-2"><Link href="/login" className="rounded-xl border px-4 py-2 text-sm">ورود</Link><Link href="/cart" className="rounded-full bg-black/10 px-4 py-2 text-sm font-bold text-[#34204f]">سبد خرید</Link></div></div></header><section className="mx-auto max-w-7xl px-5 py-14"><div className="text-sm text-black/45">فروشگاه</div><h1 className="mt-2 text-4xl font-black">همه محصولات</h1><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map(p=><Link key={p.id} href={`/products?product=${p.slug||p.id}`} className="academy-card p-4">{p.image_url?<img src={p.image_url} alt={p.title||"محصول"} className="aspect-square w-full rounded-2xl object-cover"/>:<div className="flex aspect-square items-center justify-center rounded-2xl bg-[#302d34]"><ShoppingBag className="text-black/20" size={42}/></div>}<div className="mt-4 text-xs text-black/40">{p.category}</div><h2 className="mt-1 font-bold">{p.title}</h2><div className="mt-2 font-black">{Number(p.price||0).toLocaleString("fa-IR")} تومان</div></Link>)}</div>{!products.length&&<div className="mt-10 rounded-3xl border border-dashed bg-white p-12 text-center text-black/50">محصول فعالی وجود ندارد.</div>}</section></main>}