"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import {createClient} from "../../lib/supabase/client"; import Link from "next/link";
export default function LoginPage(){const [phone,setPhone]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError("");const {error}=await createClient().auth.signInWithPassword({phone,password});if(error)setError(error.message);else router.push("/");setBusy(false)}return <main dir="rtl" className="grid min-h-screen place-items-center bg-[#f8f7f4] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-sm"><Link href="/" className="text-sm text-black/50">بازگشت به سایت</Link><h1 className="mt-5 text-3xl font-black">ورود به حساب</h1><p className="mt-2 text-black/50">برای خرید و استفاده از آموزش‌ها وارد شوید.</p><label className="mt-8 block text-sm">شماره موبایل<input required value={phone} onChange={e=>setPhone(e.target.value)} className="mt-2 w-full rounded-xl border p-3" placeholder="+98..." dir="ltr"/></label><label className="mt-4 block text-sm">رمز عبور<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border p-3" dir="ltr"/></label>{error&&<div className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<button disabled={busy} className="mt-6 w-full rounded-2xl bg-gradient-to-r from-violet-600 to-sky-500 p-3 font-bold text-white shadow-lg disabled:opacity-50">{busy?"در حال ورود...":"ورود"}</button></form><p className="mt-5 text-sm text-slate-500">حساب نداری؟ <a href="/signup" className="font-bold text-violet-700">ثبت‌نام</a></p></main>}